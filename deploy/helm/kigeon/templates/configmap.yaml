apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kigeon.fullname" . }}
  labels:
    {{- include "kigeon.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.config | toYaml | nindent 4 }}
