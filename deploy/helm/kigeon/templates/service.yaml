apiVersion: v1
kind: Service
metadata:
  name: {{ include "kigeon.fullname" . }}
  labels:
    {{- include "kigeon.labels" . | nindent 4 }}
spec:
  clusterIP: None
  selector:
    {{- include "kigeon.selectorLabels" . | nindent 4 }}
